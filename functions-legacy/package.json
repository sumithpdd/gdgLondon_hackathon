{
  "name": "functions-legacy",
  "description": "Gen-1 photobooth / DevFest functions (preserved on shared Firebase project)",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions:legacy"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
